from setuptools import setup

setup(
   name='pixel_art',
   version='0.1',
   description='Package for drawing pixel art to the terminal.',
   author='The Codio Guys (via Matt Baldwin)',
   email='mcbalz@gmail.com',
   packages=['pixel_art'],
   install_requires = ['colorama']
)
